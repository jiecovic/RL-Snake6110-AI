model:
  features_extractor:
    type: tile_vit
    features_dim: 64
    params:
      d_model: 64
      n_layers: 3
      n_heads: 8
      dropout: 0.0
      pooling: cls_mean
      pos_mode: pov_center
      use_token_mask: true
  net_arch: []
